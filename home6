package lesson6.chess;

public class Rook extends Figure {

    public Rook(int x, int y) {
        super(x, y);
    }

    @Override
    public boolean can(int x, int y) {
        //horizontal x, vertically y
        int hx = x getX();
        int hy = y getY();
        int vx = x getX();
        int vy = y getY();
        // TODO: 29.03.2021 implement rook logic
        return false;
    }
}
